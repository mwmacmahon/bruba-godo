# Bruba-godo gitignore

# Local context files (machine-specific, not committed)
LOCAL*.md

# User configuration (use config.yaml.example as template)
config.yaml

# Environment variables
.env

# Vault setup artifacts
.gitignore.pre-vault

# Vault-managed directories (symlinked when vault mode enabled)
agents
reference
exports
logs
docs/cc_logs
docs/meta
docs/packets

# Generated cronjob files (source of truth: templates/cronjobs/ + config.yaml)
cronjobs/nightly-reset-prep.yaml
cronjobs/nightly-reset-execute.yaml
cronjobs/nightly-reset-wake.yaml
cronjobs/morning-briefing.yaml

# User customizations (personal, not committed)
user/

# macOS
.DS_Store

# Editor files
*.swp
*~

# Python
__pycache__/
*.pyc
